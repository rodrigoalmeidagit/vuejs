<template>
  <footer>
    <a
      v-for="(socialLink, index) in socialLinks"
      :key="index"
      :href="socialLink.link"
      target="_blank"
      class="socialLink"
    >
      <img
        :alt="socialLink.label"
        :title="socialLink.label"
        :src="socialLink.icon"
      />
    </a>
  </footer>
</template>

<script>
import api from "@/services/api.js";
export default {
  name: "Footer",
  data() {
    return {
      socialLinks: [],
    };
  },
  mounted() {
    api.get("/social-links.json").then((res) => (this.socialLinks = res.data));
  },
};
</script>

<style scoped>
footer {
  height: 60px;
  width: 100%;
  background-color: var(--color-background-nav);
  display: flex;
  justify-content: center;
  align-items: center;
}

.socialLink {
  border: 1px solid var(--color-text-light);
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 40px;
  height: 40px;
  margin: 0 10px;
}
.socialLink img {
  width: 20px;
}
</style>
